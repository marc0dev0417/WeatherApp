import {Theme} from '@mui/material'
import {createStyles, makeStyles} from '@mui/styles'

const radioGender = makeStyles((theme: Theme) =>
    createStyles({
        root:{
            marginLeft: '70px'
        }
    })
)
export default radioGender